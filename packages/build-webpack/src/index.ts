import buildConfig from "./buildConfig";
import {
  Options as BuildOptions,
  Env as BuildEnv,
  Mode as BuildMode,
  Paths as BuildPaths
} from "./types/config";

export default buildConfig;
export type {
  BuildOptions,
  BuildEnv,
  BuildMode,
  BuildPaths
};